{
  "name": "smart-monitoring",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n API,WEB -c green,cyan \"cd backend && .venv\\Scripts\\activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload\" \"cd frontend && npm run dev -- --host\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0"
  }
}
